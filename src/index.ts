/**
 * Package entry point (`@daanvandenbergh/i18nkit`). Exposes the framework-agnostic i18n core: the
 * `I18n` class plus its types and helpers. React components live in the
 * `@daanvandenbergh/i18nkit/react` subpath.
 */

export * from "./i18n/index.js";
